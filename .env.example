# ─── DATABASE ─────────────────────────────────────────────────
DATABASE_URL=

# ─── API (apps/api) ───────────────────────────────────────────
BETTER_AUTH_SECRET=
PORT=

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Email — Outbound via Gmail SMTP
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Email — Inbound SMTP listener
INBOUND_SMTP_PORT=

# File Storage — MinIO
MINIO_ENDPOINT=
MINIO_PORT=
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=
MINIO_BUCKET=

# GitHub OAuth App
GITHUB_APP_CLIENT_ID=
GITHUB_APP_CLIENT_SECRET=
# Same value as GITHUB_APP_CLIENT_ID — exposed to dashboard frontend for OAuth redirect
# Set Authorization callback URL in GitHub OAuth App to: {DASHBOARD_URL}/settings/github/callback
NEXT_PUBLIC_GITHUB_CLIENT_ID=

# Redis
REDIS_URL=

# App URLs (used by API for CORS + email links)
PORTAL_URL=
DASHBOARD_URL=
NODE_ENV=

# ─── PORTAL / DASHBOARD (apps/portal, apps/dashboard) ─────────
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_PORTAL_URL=
NEXT_PUBLIC_ASSETS_URL=